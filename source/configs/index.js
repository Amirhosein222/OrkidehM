import { apiConfig, baseUrl } from './apiConfig';
import { COLORS, SCROLL_VIEW_CONTAINER, SNACKBAR_OPTIONS } from './styles';
import { HEIGHT, WIDTH, STATUS_BAR_HEIGHT, rw, rh } from './dimensions';
import { faLocale } from './calendarLocaleConfig';
import { months, days, getYears } from './statics';

export {
  apiConfig,
  baseUrl,
  COLORS,
  SCROLL_VIEW_CONTAINER,
  SNACKBAR_OPTIONS,
  HEIGHT,
  WIDTH,
  faLocale,
  months,
  days,
  getYears,
  STATUS_BAR_HEIGHT,
  rw,
  rh,
};
